from multiprocessing import Pool
from engine.apiclients.exchangerateclient import ExchangeRateClient

#This class is the main component, which calls the relevant services and composes the info
class InfoEngine:

    def __init__(self):
        print "initializes the info engine"

    #Returns the info
    def getInfo():
        #Exectues the api clients asynchronously
        return None

    #returns the updates
    def getInfoUpdate():
        return None
