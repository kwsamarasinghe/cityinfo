from engine.infoengine import InfoEngine

#CityInfo RESTFull service
class Server:
    
    def __init__(self, configFile):
        #Reads the config file and initializes the server
        #Initially go through the data sources an

if __name__ == '__main__':
    server = Server()
    print "server\n"
    engine = InfoEngine()
